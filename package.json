{
  "devDependencies": {
    "@types/node": "^18.11.18",
    "prettier": "2.8.2",
    "typedoc": "^0.23.23",
    "typescript": "^4.9.3"
  },
  "scripts": {
    "build-docs": "typedoc && pnpm -r test",
    "build-all": "pnpm install; pnpm --filter @b5x/types build; pnpm install; pnpm --filter @b5x/conditions-manager build; pnpm install; pnpm --filter @b5x/parser build; pnpm install; pnpm --filter @b5x/cli build; pnpm --filter @b5x/api build",
    "seed-all": "pnpm --filter @b5x/cli seed && pnpm --filter @b5x/api seed",
    "start-all": "pnpm -r devstart"
  }
}
